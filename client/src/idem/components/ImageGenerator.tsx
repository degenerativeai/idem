
import React from 'react';

const ImageGenerator: React.FC = () => {
    return (
        <div className="glass-panel" style={{ padding: '2rem', height: '100%' }}>
            <h2>Generator</h2>
            <p style={{ color: 'var(--text-secondary)' }}>Generate images using Wavespeed or Google.</p>
            {/* Implementation placeholder */}
            <div style={{ marginTop: '2rem', padding: '2rem', border: '1px dashed rgba(255,255,255,0.1)', borderRadius: '8px', textAlign: 'center', color: 'var(--text-secondary)' }}>
                Social Media / UGC Generator coming soon...
            </div>
        </div>
    );
};

export default ImageGenerator;
